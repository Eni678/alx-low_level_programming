#include "main.h"

/**
 * add - this function adds two integers and returns result
 * @a: an integer a
 * @b: an integer b
 * Return: The sum of @a and @b
 */
int add(int a, int b)
{
	return (a + b);
}
